apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
  namespace: demo
spec:
  ingressClassName: nginx-example
  rules:
  - http:
      paths:
      - path: /testpath
        backend:
          service:
            name: test
            port:
              number: 80
